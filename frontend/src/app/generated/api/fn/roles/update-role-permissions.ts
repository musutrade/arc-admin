/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UpdateRolePermissionsRequest } from '../../models/update-role-permissions-request';

export interface UpdateRolePermissions$Params {

/**
 * 角色 ID
 */
  id: number;

/**
 * 权限分配再认证凭据
 */
  'X-Step-Up-Token': string;
      body: UpdateRolePermissionsRequest
}

export function updateRolePermissions(http: HttpClient, rootUrl: string, params: UpdateRolePermissions$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, updateRolePermissions.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-Step-Up-Token', params['X-Step-Up-Token'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

updateRolePermissions.PATH = '/roles/{id}/permissions';
