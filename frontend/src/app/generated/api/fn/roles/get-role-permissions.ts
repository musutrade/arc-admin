/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RolePermissions } from '../../models/role-permissions';

export interface GetRolePermissions$Params {

/**
 * 角色 ID
 */
  id: number;
}

export function getRolePermissions(http: HttpClient, rootUrl: string, params: GetRolePermissions$Params, context?: HttpContext): Observable<StrictHttpResponse<RolePermissions>> {
  const rb = new RequestBuilder(rootUrl, getRolePermissions.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RolePermissions>;
    })
  );
}

getRolePermissions.PATH = '/roles/{id}/permissions';
