/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DepartmentResponse } from '../../models/department-response';
import { UpdateDepartmentRequest } from '../../models/update-department-request';

export interface UpdateDepartment$Params {

/**
 * 部门 ID
 */
  id: number;

/**
 * 部门管理再认证凭据
 */
  'X-Step-Up-Token': string;
      body: UpdateDepartmentRequest
}

export function updateDepartment(http: HttpClient, rootUrl: string, params: UpdateDepartment$Params, context?: HttpContext): Observable<StrictHttpResponse<DepartmentResponse>> {
  const rb = new RequestBuilder(rootUrl, updateDepartment.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-Step-Up-Token', params['X-Step-Up-Token'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DepartmentResponse>;
    })
  );
}

updateDepartment.PATH = '/departments/{id}';
