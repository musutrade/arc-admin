/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateDepartmentRequest } from '../../models/create-department-request';
import { DepartmentResponse } from '../../models/department-response';

export interface CreateDepartment$Params {

/**
 * 部门管理再认证凭据
 */
  'X-Step-Up-Token': string;
      body: CreateDepartmentRequest
}

export function createDepartment(http: HttpClient, rootUrl: string, params: CreateDepartment$Params, context?: HttpContext): Observable<StrictHttpResponse<DepartmentResponse>> {
  const rb = new RequestBuilder(rootUrl, createDepartment.PATH, 'post');
  if (params) {
    rb.header('X-Step-Up-Token', params['X-Step-Up-Token'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DepartmentResponse>;
    })
  );
}

createDepartment.PATH = '/departments';
