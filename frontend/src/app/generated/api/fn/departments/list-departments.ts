/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DepartmentResponse } from '../../models/department-response';

export interface ListDepartments$Params {
}

export function listDepartments(http: HttpClient, rootUrl: string, params?: ListDepartments$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<DepartmentResponse>>> {
  const rb = new RequestBuilder(rootUrl, listDepartments.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<DepartmentResponse>>;
    })
  );
}

listDepartments.PATH = '/departments';
