/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface DeleteDepartment$Params {

/**
 * 部门 ID
 */
  id: number;

/**
 * 部门删除再认证凭据
 */
  'X-Step-Up-Token': string;
}

export function deleteDepartment(http: HttpClient, rootUrl: string, params: DeleteDepartment$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, deleteDepartment.PATH, 'delete');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-Step-Up-Token', params['X-Step-Up-Token'], {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

deleteDepartment.PATH = '/departments/{id}';
