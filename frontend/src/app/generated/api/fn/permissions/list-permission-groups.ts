/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PermissionGroupResponse } from '../../models/permission-group-response';

export interface ListPermissionGroups$Params {
}

export function listPermissionGroups(http: HttpClient, rootUrl: string, params?: ListPermissionGroups$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<PermissionGroupResponse>>> {
  const rb = new RequestBuilder(rootUrl, listPermissionGroups.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<PermissionGroupResponse>>;
    })
  );
}

listPermissionGroups.PATH = '/permissions/groups';
