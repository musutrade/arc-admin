/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DashboardStats } from '../../models/dashboard-stats';

export interface GetDashboardStats$Params {
}

export function getDashboardStats(http: HttpClient, rootUrl: string, params?: GetDashboardStats$Params, context?: HttpContext): Observable<StrictHttpResponse<DashboardStats>> {
  const rb = new RequestBuilder(rootUrl, getDashboardStats.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DashboardStats>;
    })
  );
}

getDashboardStats.PATH = '/dashboard/stats';
