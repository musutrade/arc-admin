/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageUser } from '../../models/page-user';
import { SortDirection } from '../../models/sort-direction';
import { UserSortBy } from '../../models/user-sort-by';
import { UserStatus } from '../../models/user-status';

export interface ListUsers$Params {
  page?: number;
  pageSize?: number;
  keyword?: string;
  status?: UserStatus;
  role?: string;
  sortBy?: UserSortBy;
  sortDirection?: SortDirection;
}

export function listUsers(http: HttpClient, rootUrl: string, params?: ListUsers$Params, context?: HttpContext): Observable<StrictHttpResponse<PageUser>> {
  const rb = new RequestBuilder(rootUrl, listUsers.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('keyword', params.keyword, {});
    rb.query('status', params.status, {});
    rb.query('role', params.role, {});
    rb.query('sortBy', params.sortBy, {});
    rb.query('sortDirection', params.sortDirection, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageUser>;
    })
  );
}

listUsers.PATH = '/users';
