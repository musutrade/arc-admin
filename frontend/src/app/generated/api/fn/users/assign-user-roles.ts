/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AssignRolesRequest } from '../../models/assign-roles-request';

export interface AssignUserRoles$Params {

/**
 * 用户 ID
 */
  id: number;

/**
 * 角色分配再认证凭据
 */
  'X-Step-Up-Token': string;
      body: AssignRolesRequest
}

export function assignUserRoles(http: HttpClient, rootUrl: string, params: AssignUserRoles$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, assignUserRoles.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-Step-Up-Token', params['X-Step-Up-Token'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

assignUserRoles.PATH = '/users/{id}/roles';
