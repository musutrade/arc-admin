/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BatchUserIdsRequest } from '../../models/batch-user-ids-request';

export interface BatchDeleteUsers$Params {

/**
 * 用户删除再认证凭据
 */
  'X-Step-Up-Token': string;
      body: BatchUserIdsRequest
}

export function batchDeleteUsers(http: HttpClient, rootUrl: string, params: BatchDeleteUsers$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, batchDeleteUsers.PATH, 'post');
  if (params) {
    rb.header('X-Step-Up-Token', params['X-Step-Up-Token'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

batchDeleteUsers.PATH = '/users/batch-delete';
