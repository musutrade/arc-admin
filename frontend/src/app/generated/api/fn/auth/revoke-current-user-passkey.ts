/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MfaFactorRevokeRequest } from '../../models/mfa-factor-revoke-request';

export interface RevokeCurrentUserPasskey$Params {

/**
 * 通行密钥 ID
 */
  id: number;
      body: MfaFactorRevokeRequest
}

export function revokeCurrentUserPasskey(http: HttpClient, rootUrl: string, params: RevokeCurrentUserPasskey$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, revokeCurrentUserPasskey.PATH, 'delete');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

revokeCurrentUserPasskey.PATH = '/auth/me/mfa/passkey/{id}';
