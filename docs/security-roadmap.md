# 安全能力待办

## `super_admin` 多因素认证

- [x] 状态：已实现。`super_admin` 登录强制完成 MFA，未验证前不创建认证会话。
- [x] 注册：支持 WebAuthn 安全密钥或通行密钥，并提供 TOTP 兼容方案；TOTP 使用 AES-256-GCM 加密，恢复码只保存 Argon2 哈希。
- [x] 登录：密码校验后进入独立二次验证阶段，未通过前不得签发完整认证会话；二次验证挑战在 PostgreSQL 中独立限流。
- [x] 强制策略：所有 `super_admin` 强制注册并验证，不允许通过记住设备、密码重置或管理员接口绕过。
- [x] 恢复与撤销：敏感操作需要当前密码和 TOTP，撤销通行密钥、重新生成或使用恢复码后立即撤销该用户全部旧会话。
- [x] 审计：记录注册、验证成功/失败、因子撤销、恢复码使用和策略变更，不记录密钥、验证码或恢复码原文。
- [x] 验收：已补齐迁移、后端接口、Angular 管理界面、OpenAPI 契约、集成测试和[生产运维说明](mfa-operations.md)。
